import { LitElement, html } from "lit";
import { customElement } from "lit/decorators.js";

import { styles } from "./grid.styles";

@customElement("bx-grid")
export class Grid extends LitElement {
    static styles = styles;

    render() {
        return html``;
    }
}
